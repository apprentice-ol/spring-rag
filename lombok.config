# Copy @Qualifier on fields into the constructor generated by @RequiredArgsConstructor,
# so constructor injection routes to the intended bean deterministically.
# Ref: https://projectlombok.org/features/configuration
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
