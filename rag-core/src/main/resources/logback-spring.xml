<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Spring Boot 默认变量和 CONSOLE appender（含 traceId 自动注入） -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <!-- OTLP Logs appender。
         日志统一走 OpenTelemetry Logs：
         - obs.collector.enabled=true  -> management.otlp.logging.endpoint 指向 otel-collector
         - obs.collector.enabled=false -> management.otlp.logging.endpoint 指向 OpenObserve /v1/logs
         具体端点由 ObsEnvironmentPostProcessor 启动时桥接。
         旧的 OpenObserveAppender 不再启用，保留类文件仅作为历史实现。 -->
    <appender name="OPEN_TELEMETRY"
              class="io.opentelemetry.instrumentation.logback.appender.v1_0.OpenTelemetryAppender">
        <captureExperimentalAttributes>true</captureExperimentalAttributes>
    </appender>

    <!-- 日志级别由 application.yaml 的 logging.level 控制 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="OPEN_TELEMETRY"/>
    </root>
</configuration>
